{
  "name": "@unbox-box/tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Typed tool registry, analysis functions and the command engine shared by the UI, WebMCP and the MCP server.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "@vitest/coverage-v8": "^5.0.1",
    "vitest": "^5.0.1"
  },
  "sideEffects": [
    "./src/zod-setup.ts"
  ]
}
